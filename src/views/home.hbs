
        <section id="viewCatalog" class="background-img">
            <div class="added-hotels">
                {{#each allHotels}}
                {{> hotel}}
                {{else}}
                <div class="guest">
                There are no Hotels found...
               </div>
                {{/each}}
            </div>
        </section>

